extends admin_layout

block content
    h1 Delete Admin
    p Are you sure you want to delete the Admin "#{admin.username}"?
    form(action=`/my-sat/admin/${admin._id}/delete` method="POST")
      button.btn.btn-danger(type="submit") Yes, Delete
      a.btn.btn-secondary(href="/my-sat/manage_admins") Cancel
